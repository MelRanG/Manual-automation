# DocOps AI Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a full-stack Document Lifecycle Automation Platform (DocOps AI) with RAG-based Q&A, error reporting, AI correction proposals, approval workflows, trust scoring, SR generation, and change merge assistant.

**Architecture:** FastAPI backend with PostgreSQL + pgvector for document storage and vector search. React + Vite frontend with shadcn/ui. LLM integration via abstracted service layer (mock by default, swap to real API via env). Docker Compose for local development.

**Tech Stack:** Python 3.12, FastAPI, SQLAlchemy 2.0, pgvector, React 19, TypeScript, Vite, shadcn/ui, Tailwind CSS, Docker

---

## Task 1: Database Schema + Docker Infrastructure

**Files:**
- Create: `docker-compose.yml` (replace existing)
- Create: `backend/app/models/__init__.py`
- Create: `backend/app/models/base.py`
- Create: `backend/app/models/user.py`
- Create: `backend/app/models/document.py`
- Create: `backend/app/models/chat.py`
- Create: `backend/app/models/feedback.py`
- Create: `backend/app/models/sr.py`
- Create: `backend/app/db.py`
- Create: `backend/alembic.ini`
- Create: `backend/alembic/env.py`
- Create: `backend/alembic/versions/001_initial.py`
- Create: `.env.example`
- Modify: `backend/pyproject.toml`

- [ ] **Step 1: Update docker-compose.yml with PostgreSQL + pgvector**

```yaml
services:
  db:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_DB: docops
      POSTGRES_USER: docops
      POSTGRES_PASSWORD: docops_dev
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U docops"]
      interval: 5s
      timeout: 3s
      retries: 5

  backend:
    build: ./backend
    ports:
      - "8000:8000"
    env_file:
      - .env
    depends_on:
      db:
        condition: service_healthy

  frontend:
    build: ./frontend
    ports:
      - "80:80"
    depends_on:
      - backend

volumes:
  pgdata:
```

- [ ] **Step 2: Create .env.example**

```env
DATABASE_URL=postgresql+asyncpg://docops:docops_dev@localhost:5432/docops
LLM_PROVIDER=mock
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
EMBEDDING_MODEL=mock
JIRA_WEBHOOK_URL=
SECRET_KEY=change-me-in-production
```

- [ ] **Step 3: Add backend dependencies**

Run: `cd backend && uv add sqlalchemy[asyncio] asyncpg alembic pgvector pydantic-settings python-multipart aiofiles`

- [ ] **Step 4: Create database connection module**

`backend/app/db.py`:
```python
from sqlalchemy.ext.asyncio import create_async_engine, async_sessionmaker, AsyncSession
from sqlalchemy.orm import DeclarativeBase
from app.config import settings

engine = create_async_engine(settings.database_url)
SessionLocal = async_sessionmaker(engine, class_=AsyncSession, expire_on_commit=False)

class Base(DeclarativeBase):
    pass

async def get_db():
    async with SessionLocal() as session:
        yield session
```

- [ ] **Step 5: Create config module**

`backend/app/config.py`:
```python
from pydantic_settings import BaseSettings

class Settings(BaseSettings):
    database_url: str = "postgresql+asyncpg://docops:docops_dev@localhost:5432/docops"
    llm_provider: str = "mock"
    anthropic_api_key: str = ""
    openai_api_key: str = ""
    embedding_model: str = "mock"
    jira_webhook_url: str = ""
    secret_key: str = "change-me-in-production"

    class Config:
        env_file = ".env"

settings = Settings()
```

- [ ] **Step 6: Create all SQLAlchemy models**

Models as defined in the service spec:
- User, Document, DocumentVersion, DocumentChunk
- ChatSession, ChatMessage, AnswerCitation
- FeedbackReport, ProposedDocumentChange, ApprovalRequest
- SRDraft, WebhookDeliveryLog
- ChangeImpactAnalysis, DocumentChangeProposal

Each with proper relationships, indexes, and pgvector column for embeddings.

- [ ] **Step 7: Setup Alembic and create initial migration**

Run: `cd backend && uv run alembic init alembic`
Then create initial migration with all models.

- [ ] **Step 8: Run migration against local DB**

Run: `docker compose up db -d && cd backend && uv run alembic upgrade head`
Expected: All tables created successfully.

- [ ] **Step 9: Write test for DB connection**

```python
# tests/test_db.py
import pytest
from app.db import engine

@pytest.mark.asyncio
async def test_db_connection():
    async with engine.connect() as conn:
        result = await conn.execute(text("SELECT 1"))
        assert result.scalar() == 1
```

- [ ] **Step 10: Verify and commit**

Run: `cd backend && uv run pytest tests/test_db.py -v`
Expected: PASS

```bash
git add -A
git commit -m "feat: database schema and Docker infrastructure"
```

---

## Task 2: Document Upload & Storage API

**Files:**
- Create: `backend/app/routers/__init__.py`
- Create: `backend/app/routers/documents.py`
- Create: `backend/app/schemas/documents.py`
- Create: `backend/app/services/document_service.py`
- Create: `backend/uploads/` (directory)
- Create: `tests/test_documents.py`
- Modify: `backend/app/main.py`

- [ ] **Step 1: Write failing tests for document CRUD**

```python
# tests/test_documents.py
def test_upload_document():
    # POST /api/documents with file upload
    # expect 201, document metadata returned

def test_list_documents():
    # GET /api/documents
    # expect 200, list of documents

def test_get_document():
    # GET /api/documents/{id}
    # expect 200, document with versions
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `cd backend && uv run pytest tests/test_documents.py -v`
Expected: FAIL (routes don't exist yet)

- [ ] **Step 3: Create Pydantic schemas**

- [ ] **Step 4: Create document service layer**

- [ ] **Step 5: Create document router with endpoints**

Endpoints:
- `POST /api/documents` — upload file (txt, md, pdf)
- `GET /api/documents` — list all documents
- `GET /api/documents/{id}` — get document detail with versions
- `GET /api/documents/{id}/versions` — version history
- `PATCH /api/documents/{id}` — update metadata (title, owner)
- `DELETE /api/documents/{id}` — soft delete

- [ ] **Step 6: Register router in main.py**

- [ ] **Step 7: Run tests to verify they pass**

Run: `cd backend && uv run pytest tests/test_documents.py -v`
Expected: PASS

- [ ] **Step 8: Commit**

```bash
git add -A
git commit -m "feat: document upload and storage API"
```

---

## Task 3: Document Chunking + Vector Embedding

**Files:**
- Create: `backend/app/services/chunking_service.py`
- Create: `backend/app/services/embedding_service.py`
- Create: `backend/app/services/llm/__init__.py`
- Create: `backend/app/services/llm/base.py`
- Create: `backend/app/services/llm/mock.py`
- Create: `backend/app/services/llm/anthropic.py`
- Create: `backend/app/services/llm/openai_provider.py`
- Create: `tests/test_chunking.py`
- Create: `tests/test_embedding.py`

- [ ] **Step 1: Write failing tests for chunking**

```python
def test_chunk_text_document():
    # Given a text document content
    # When chunked
    # Then returns list of chunks with metadata

def test_chunk_markdown_document():
    # Markdown split by headers

def test_chunk_respects_max_size():
    # No chunk exceeds max token limit
```

- [ ] **Step 2: Run tests — expect FAIL**

- [ ] **Step 3: Implement chunking service**

Strategy: split by paragraphs/headers, overlap 50 tokens, max 500 tokens per chunk.

- [ ] **Step 4: Write failing tests for embedding**

```python
def test_mock_embedding_returns_vector():
    # Mock provider returns consistent-dimension vector

def test_embed_chunks_stores_in_db():
    # After embedding, DocumentChunk records have non-null embedding
```

- [ ] **Step 5: Implement LLM abstraction layer**

Base interface → Mock implementation (deterministic random vectors) → Real implementations (Claude/OpenAI) that activate when API key is set.

- [ ] **Step 6: Implement embedding service**

- [ ] **Step 7: Integrate chunking into document upload flow**

After document upload → auto-chunk → auto-embed → store chunks.

- [ ] **Step 8: Run all tests**

Run: `cd backend && uv run pytest -v`
Expected: All PASS

- [ ] **Step 9: Commit**

```bash
git add -A
git commit -m "feat: document chunking and vector embedding with LLM abstraction"
```

---

## Task 4: Chat Q&A with Source Citations

**Files:**
- Create: `backend/app/routers/chat.py`
- Create: `backend/app/schemas/chat.py`
- Create: `backend/app/services/chat_service.py`
- Create: `backend/app/services/search_service.py`
- Create: `tests/test_chat.py`
- Create: `tests/test_search.py`

- [ ] **Step 1: Write failing tests for vector search**

```python
def test_search_returns_relevant_chunks():
    # Given embedded documents
    # When searching with a query
    # Then returns top-k relevant chunks with scores

def test_search_empty_db_returns_empty():
    # No documents → empty results
```

- [ ] **Step 2: Write failing tests for chat**

```python
def test_create_chat_session():
    # POST /api/chat/sessions → 201

def test_send_message_gets_ai_response():
    # POST /api/chat/sessions/{id}/messages
    # Returns AI response with citations

def test_response_includes_source_documents():
    # Citations link to specific document chunks

def test_no_documents_found_response():
    # AI says "문서에서 확인되지 않음"
```

- [ ] **Step 3: Implement search service (pgvector similarity search)**

- [ ] **Step 4: Implement chat service (LLM generates answer from context)**

- [ ] **Step 5: Create chat router**

Endpoints:
- `POST /api/chat/sessions` — create session
- `GET /api/chat/sessions` — list sessions
- `GET /api/chat/sessions/{id}` — get session with messages
- `POST /api/chat/sessions/{id}/messages` — send message, get AI response

- [ ] **Step 6: Run all tests**

Run: `cd backend && uv run pytest -v`
Expected: All PASS

- [ ] **Step 7: Commit**

```bash
git add -A
git commit -m "feat: chat Q&A with vector search and source citations"
```

---

## Task 5: Error Reporting + AI Correction Proposals

**Files:**
- Create: `backend/app/routers/feedback.py`
- Create: `backend/app/schemas/feedback.py`
- Create: `backend/app/services/feedback_service.py`
- Create: `backend/app/services/correction_service.py`
- Create: `tests/test_feedback.py`

- [ ] **Step 1: Write failing tests**

```python
def test_create_feedback_report():
    # POST /api/feedback with chat_message_id + feedback_text
    # Returns 201 with report

def test_feedback_triggers_correction_proposal():
    # After feedback created, AI generates ProposedDocumentChange
    # With original_text, proposed_text, diff, reasoning, confidence

def test_feedback_links_to_document_and_chunk():
    # Report correctly references source document
```

- [ ] **Step 2: Run tests — expect FAIL**

- [ ] **Step 3: Implement feedback service**

- [ ] **Step 4: Implement correction service**

AI compares original chunk content with user's feedback, generates:
- proposed_text (corrected version)
- diff (what changed)
- reasoning (why this correction)
- confidence (0.0-1.0)

- [ ] **Step 5: Create feedback router**

Endpoints:
- `POST /api/feedback` — submit error report
- `GET /api/feedback` — list all reports (with status filter)
- `GET /api/feedback/{id}` — report detail with proposed change

- [ ] **Step 6: Run all tests**

- [ ] **Step 7: Commit**

```bash
git add -A
git commit -m "feat: error reporting and AI correction proposal generation"
```

---

## Task 6: Approval Workflow

**Files:**
- Create: `backend/app/routers/approvals.py`
- Create: `backend/app/schemas/approvals.py`
- Create: `backend/app/services/approval_service.py`
- Create: `tests/test_approvals.py`

- [ ] **Step 1: Write failing tests**

```python
def test_list_pending_approvals():
    # GET /api/approvals?status=pending

def test_approve_correction():
    # POST /api/approvals/{id}/approve
    # Creates new DocumentVersion
    # Updates document current_version
    # Resolves feedback report

def test_reject_correction():
    # POST /api/approvals/{id}/reject
    # Feedback status → rejected

def test_approve_with_edit():
    # POST /api/approvals/{id}/approve with modified_text
    # Uses reviewer's text instead of AI's
```

- [ ] **Step 2: Run tests — expect FAIL**

- [ ] **Step 3: Implement approval service**

On approve:
1. Create new DocumentVersion with proposed content
2. Update Document.current_version_id
3. Re-chunk and re-embed the new version
4. Update FeedbackReport.status → resolved
5. Trigger RAG re-index

- [ ] **Step 4: Create approval router**

- [ ] **Step 5: Run all tests**

- [ ] **Step 6: Commit**

```bash
git add -A
git commit -m "feat: approval workflow with document version creation"
```

---

## Task 7: Document Trust Score

**Files:**
- Create: `backend/app/services/trust_score_service.py`
- Create: `backend/app/routers/admin.py`
- Create: `tests/test_trust_score.py`

- [ ] **Step 1: Write failing tests**

```python
def test_new_document_has_default_score():
    # Fresh document → trust_score = 1.0

def test_feedback_reports_decrease_score():
    # Each unresolved feedback → -0.1

def test_resolved_feedback_recovers_score():
    # Resolved feedback → partial recovery

def test_old_document_lower_score():
    # last_reviewed_at > 90 days → penalty

def test_no_owner_penalty():
    # document without owner_id → penalty

def test_trust_status_mapping():
    # score > 0.8 → high, > 0.6 → medium, > 0.4 → low, else → stale
```

- [ ] **Step 2: Run tests — expect FAIL**

- [ ] **Step 3: Implement trust score calculation**

Factors:
- Base: 1.0
- Unresolved feedback: -0.1 each (max -0.4)
- Pending approvals: -0.05 each (max -0.2)
- Days since last review: -0.01 per 30 days (max -0.3)
- No owner: -0.15
- Recent resolved feedback: +0.05 each (max +0.15)

- [ ] **Step 4: Add trust score recalculation triggers**

Recalculate on: feedback created, feedback resolved, approval completed, periodic job.

- [ ] **Step 5: Admin dashboard endpoint**

`GET /api/admin/dashboard` — returns:
- Low trust documents
- Documents with most feedback
- Pending approvals count
- Stale documents (no review > 90 days)
- Documents without owners

- [ ] **Step 6: Run all tests**

- [ ] **Step 7: Commit**

```bash
git add -A
git commit -m "feat: document trust scoring with admin dashboard data"
```

---

## Task 8: SR Draft Generation + Webhook

**Files:**
- Create: `backend/app/routers/sr.py`
- Create: `backend/app/schemas/sr.py`
- Create: `backend/app/services/sr_service.py`
- Create: `backend/app/services/webhook_service.py`
- Create: `tests/test_sr.py`
- Create: `tests/test_webhook.py`

- [ ] **Step 1: Write failing tests**

```python
def test_generate_sr_draft():
    # POST /api/sr/generate with natural language input
    # Returns structured SR draft (title, description, priority, etc.)

def test_send_sr_via_webhook():
    # POST /api/sr/{id}/send
    # If JIRA_WEBHOOK_URL set → sends HTTP POST
    # If not set → logs internally, returns success with "mock" flag

def test_webhook_delivery_logged():
    # After send, WebhookDeliveryLog record created
```

- [ ] **Step 2: Run tests — expect FAIL**

- [ ] **Step 3: Implement SR service**

AI generates structured SR from natural language:
- title, description, priority, related docs, confirmation items

- [ ] **Step 4: Implement webhook service**

```python
async def send_webhook(sr_draft: SRDraft) -> WebhookDeliveryLog:
    if not settings.jira_webhook_url:
        # Log internally, mark as "mock_delivered"
        return log_entry
    # Real HTTP POST to webhook URL
    # Log response status and body
```

- [ ] **Step 5: Create SR router**

Endpoints:
- `POST /api/sr/generate` — generate SR draft from text
- `GET /api/sr` — list SR drafts
- `GET /api/sr/{id}` — SR detail
- `POST /api/sr/{id}/send` — send via webhook
- `PATCH /api/sr/{id}` — edit draft before sending

- [ ] **Step 6: Run all tests**

- [ ] **Step 7: Commit**

```bash
git add -A
git commit -m "feat: SR draft generation with webhook delivery (mock/real)"
```

---

## Task 9: Change Merge Assistant

**Files:**
- Create: `backend/app/routers/change_merge.py`
- Create: `backend/app/schemas/change_merge.py`
- Create: `backend/app/services/change_merge_service.py`
- Create: `tests/test_change_merge.py`

- [ ] **Step 1: Write failing tests**

```python
def test_analyze_change_impact():
    # POST /api/change-merge/analyze with SR completion data
    # Returns related documents + recommended strategy

def test_strategy_types():
    # partial_update, overwrite, create_new, hold

def test_generate_document_change_proposal():
    # For partial_update strategy, generates diff

def test_approve_change_proposal():
    # Creates new DocumentVersion, re-indexes
```

- [ ] **Step 2: Run tests — expect FAIL**

- [ ] **Step 3: Implement change merge service**

AI analyzes:
- SR completion content vs existing documents (vector similarity)
- Determines affected documents
- Recommends strategy with reasoning and confidence

- [ ] **Step 4: Create change merge router**

Endpoints:
- `POST /api/change-merge/analyze` — analyze change impact
- `GET /api/change-merge/{id}` — get analysis detail
- `POST /api/change-merge/{id}/proposals` — generate document change proposals
- `POST /api/change-merge/{id}/proposals/{pid}/approve` — approve and apply

- [ ] **Step 5: Run all tests**

- [ ] **Step 6: Commit**

```bash
git add -A
git commit -m "feat: change merge assistant with impact analysis"
```

---

## Task 10: Frontend — Full UI

**Files:**
- Recreate: `frontend/` (clean Vite + React + TypeScript + shadcn/ui + Tailwind + React Router)
- Create: `frontend/src/pages/` (all pages)
- Create: `frontend/src/components/` (shared components)
- Create: `frontend/src/api/` (API client)
- Create: `frontend/src/hooks/` (custom hooks)

- [ ] **Step 1: Setup shadcn/ui + Tailwind + React Router**

- [ ] **Step 2: Create layout with sidebar navigation**

Pages:
1. 문서함 (Document Library)
2. 문서 상세 (Document Detail + Versions)
3. AI 문의 챗봇 (Chat)
4. 오류 제보함 (Feedback Reports)
5. 수정 검토함 (Approval Queue)
6. SR 생성 (SR Draft)
7. 변경 반영 검토 (Change Merge)
8. 관리자 대시보드 (Admin Dashboard)

- [ ] **Step 3: Implement Document Library page**

- [ ] **Step 4: Implement Chat page with streaming-like UX**

- [ ] **Step 5: Implement Feedback & Approval pages**

- [ ] **Step 6: Implement SR and Change Merge pages**

- [ ] **Step 7: Implement Admin Dashboard**

- [ ] **Step 8: API client with proper error handling**

- [ ] **Step 9: Verify full build**

Run: `cd frontend && pnpm build`
Expected: exit 0, no TypeScript errors

- [ ] **Step 10: Commit**

```bash
git add -A
git commit -m "feat: complete frontend UI with all pages"
```

---

## Task 11: Integration Test + Docker Compose Verification

**Files:**
- Create: `tests/integration/test_full_loop.py`
- Modify: `docker-compose.yml` (final adjustments)

- [ ] **Step 1: Write integration test for the golden path**

```python
async def test_full_document_lifecycle():
    # 1. Upload document
    # 2. Ask question → get answer with citation
    # 3. Report error on answer
    # 4. Verify correction proposal generated
    # 5. Approve correction
    # 6. Verify new version created
    # 7. Ask same question → get updated answer
    # 8. Check trust score recovered
```

- [ ] **Step 2: Run integration test**

- [ ] **Step 3: Verify docker compose up works end-to-end**

Run: `docker compose up --build -d && sleep 10 && curl http://localhost:8000/health`
Expected: `{"status": "ok"}`

- [ ] **Step 4: Final commit**

```bash
git add -A
git commit -m "feat: integration tests and docker compose verification"
```
